package org.yanavybori.core.navigation

enum class RootRoute { HOME, OBSERVER, VOTER, WORK_PRESSURE, SETTINGS }

enum class ObserverRoute {
    HOME,
    CHECKLIST,
    SITUATIONS,
    JOURNAL,
    COUNTERS,
    RECONCILIATION,
    COMPLAINTS,
    LAWS,
    POLICE,
    PROTOCOL,
    REFERENCES,
    EMERGENCY_CONTACTS,
}
